# MarketDataPipeline
Build an end-to-end market data pipeline that pulls intraday price data from a real API, stores it, transforms it with both pandas and Polars, and benchmarks performance on realistic analytics tasks (rolling features, resampling, etc).
